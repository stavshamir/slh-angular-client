import { ImgHoverEnlargeDirective } from './img-hover-enlarge.directive';

describe('ImgHoverEnlargeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgHoverEnlargeDirective();
    expect(directive).toBeTruthy();
  });
});
